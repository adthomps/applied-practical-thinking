# Local Development

- Start frontend: `cd apps/web && npm install && npm run dev`
- Start backend: `cd apps/worker && npm install && npm run dev`
- Use two terminals for local dev
- Use Vite proxy for /api to backend
