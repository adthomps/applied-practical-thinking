# Migrate to APT Design

- Refactor all UI to use Apt\* components
- Move all business logic to services or packages/core
- Use only tokens from packages/config
- Remove all custom colors, fonts, or spacing
