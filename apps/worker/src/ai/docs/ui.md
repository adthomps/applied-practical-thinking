# UI Components

APT UI is built from Apt* components only. No direct HTML or raw Tailwind classes allowed in app code.

## Components
- `AptButton`: All buttons
- `AptCard`: Card containers
- `AptTag`: Labels/tags
- `HeroCard`: Page heroes

## Usage
- Import from `@apt/ui`
- Compose only presentational components
